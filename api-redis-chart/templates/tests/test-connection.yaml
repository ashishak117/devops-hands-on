apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-redis-chart.fullname" . }}-test-connection"
  labels:
    {{- include "api-redis-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-redis-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
